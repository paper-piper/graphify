export const config = {
  db: {
    host: '127.0.0.1',
    port: 6000,
    database: 'graphify',
    user: 'postgres',
    password: 'db-chef',
  },
  connection: {
    host: '127.0.0.1',
    port: 3000
  }
}
